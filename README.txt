This is a simple SSH brute forcer tool made with Python3
This tool require 2 additional libraries to be installed before run:
 paramiko
 termcolor 
This tool takes the ip, username and a file containing passwords as input.
